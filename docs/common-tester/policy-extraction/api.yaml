schemaVersion: 1
kind: api-policy-extraction-rules
purpose: Reserved category for request query, payload, response binding, and error handling policies.
status: scaffold
agentRules:
  - Add structured categories here before API-bound UI tests are generated.
