schemaVersion: 1
kind: selection-policy-extraction-rules
purpose: Reserved category for select, radio, checkbox, switch, tree, and picker policies.
status: scaffold
agentRules:
  - Add structured categories here before selection tests are generated.
